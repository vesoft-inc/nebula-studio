export enum PageRoute {
  GraphType = 'graphtype',
  ImportData = 'importer',
  Console = 'console',
}

export enum VisualEditorType {
  Tag = 'tag',
  Edge = 'edge',
}

export enum PropertyDataType {
  STRING = 'STRING',
  DATE = 'DATE',
  INT = 'INT',
  BOOL = 'BOOL',
}
