import type { Resource } from '../en_US';
import common from './common';
import login from './login';
import graphtype from './graphtype';

const resource: Resource = { common, login, graphtype };

export default resource;
