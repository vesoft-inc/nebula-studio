syntax = "v1"

type (
	GetTestReq  {}
	GetTestResp {
		Data interface{} `json:"data,omitempty"`
	}
)

@server (
	group:  test // is used to put related logic in the same package
	prefix: /api-studio
)
service studio-api {
	@doc "test hello world"
	@handler TestHellworld
	get /test returns (GetTestResp)
}

